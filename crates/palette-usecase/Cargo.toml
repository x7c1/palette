[package]
name = "palette-usecase"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
palette-core = { path = "../palette-core" }
palette-domain = { path = "../palette-domain" }
chrono.workspace = true
