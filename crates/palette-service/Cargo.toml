[package]
name = "palette-service"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
palette-db = { path = "../palette-db" }
palette-domain = { path = "../palette-domain" }
palette-fs = { path = "../palette-fs" }

[dev-dependencies]
serde_yaml.workspace = true
