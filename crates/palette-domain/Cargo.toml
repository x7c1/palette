[package]
name = "palette-domain"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[features]
default = []
serde = ["dep:serde"]

[dependencies]
chrono.workspace = true
serde = { workspace = true, optional = true }
uuid.workspace = true
