# Step B

## Goal

Create `step-b.txt` in the workspace root with the content "Step B done" and commit it.

## Steps

- Create the file `step-b.txt` at the workspace root
- Write the exact text: `Step B done`
- Stage and commit the file on the current branch

## Acceptance Criteria

- `step-b.txt` exists at the workspace root
- File contains exactly "Step B done"
- The file is committed (not just staged)
