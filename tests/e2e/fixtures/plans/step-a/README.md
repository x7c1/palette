# Step A

## Goal

Create `step-a.txt` in the workspace root with the content "Step A done" and commit it.

## Steps

- Create the file `step-a.txt` at the workspace root
- Write the exact text: `Step A done`
- Stage and commit the file on the current branch

## Acceptance Criteria

- `step-a.txt` exists at the workspace root
- File contains exactly "Step A done"
- The file is committed (not just staged)
