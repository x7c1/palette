# Implement

## Goal

Create `implement.txt` in the workspace root with the content "Implementation done" and commit it.

## Steps

- Create the file `implement.txt` at the workspace root
- Write the exact text: `Implementation done`
- Stage and commit the file on the current branch

## Acceptance Criteria

- `implement.txt` exists at the workspace root
- File contains exactly "Implementation done"
- The file is committed (not just staged)
